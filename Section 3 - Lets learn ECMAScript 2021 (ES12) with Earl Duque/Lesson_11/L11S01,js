//
// L11S01 - Basic Arrow Function syntax
//

// Traditional function syntax
function add(a, b) {
    return a + b;
}

// Arrow function syntax
const addArrow = (a, b) => a + b;
gs.info('Addition using Arrow Function: ' + addArrow(5, 3)); // Outputs: 8