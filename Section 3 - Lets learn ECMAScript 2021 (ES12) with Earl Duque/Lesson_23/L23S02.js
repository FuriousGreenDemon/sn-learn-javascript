//
// L23S02 - Array.of()
//

// Creating arrays with Array.of()
let numbers = Array.of(1, 2, 3);
gs.info('Numbers array: ' + numbers.join(', ')); // Outputs: 1, 2, 3

let words = Array.of('ServiceNow', 'Scripting');
gs.info('Words array: ' + words.join(', ')); // Outputs: ServiceNow, Scripting
